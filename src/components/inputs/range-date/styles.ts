import styled from 'styled-components';

import { Wrapper as DateWrapper } from '../date/styles';

export {
  Wrapper as DateWrapper,
  CalendarIcon,
  CalendarWrapper
} from '../date/styles';

export const Wrapper = styled(DateWrapper)`
  input {
    font-size: 14px;
  }
`;
